# Prima.

## A Protocol for Embodied Intelligence

**Author attribution in source conversation:** Allison.

## Abstract

The internet gave programmable money to code.
We give programmable value to the body.

Prima is a sovereign protocol that treats human movement as a high-resolution, tamper-evident oracle. It fuses choreographic mathematics (Py.rouette), executable ritual (ChoreoCode), kinematic governance (Glissé Engine), non-extractive throttling (Bell Protocol), and regenerative economics (Sentient Cents + Streetcred) into one living system.

Where Ethereum minimized trust between strangers, Prima restores trust within the self — and between the self and the ledger.

- The body does not speculate.
- The body cannot be Sybiled when ritual enforcement is active.
- The body remembers in fascia, angular momentum, and microsecond instability.

This is not move-to-earn.
This is **proof-of-embodied-intelligence**.
The plié is the new consensus.

## 1) Introduction

For over a decade, blockchain discourse centered on one question:

> How do we coordinate strangers without trusted intermediaries?

Prima asks the complementary question:

> What happens when the most sovereign, high-fidelity, biologically rooted signal generator on Earth — the trained human body — is granted cryptographic custody of its own output?

In this framing:

- Every grand plié is a state transition.
- Every honest glitch is a high-value learning event.
- Every sustained line is auditable lineage.

Prima is the protocol that listens.

## 2) The Body as a State Transition System

Bitcoin proved a simple state machine (UTXOs) can be secured by proof-of-work.
Prima proposes a richer state machine — the trained human body — secured by proof-of-presence.

### Core state variables (Py.rouette)

- **TES (Technical Element Score)** = Base Value × Difficulty Delta × Grade of Execution
- **PCS (Program Component Score)** = weighted axes (line, musicality, presence, etc.)
- **Glitch Factor** = notarized deviation (Δω > 0.15 rad/s) → Sentient Cents mint at 1:0.618

### Execution engine (Glissé FSM)

`IDLE → PREP → JETÉ → FERMATA → CODA_PROFIT / CODA_STOP → CORPS_RESET`

Transitions are gated by physical signals:

- VZ (velocity z-score)
- AZ (acceleration z-score)
- M (persistence quorum)
- Thermal shift
- Glitch confirmation

No external validator can forge a fouetté axis.
The body is the validator.

## 3) ChoreoCode: The Scripting Layer

```txt
FIFTH()                  // neutral state initialization
PASSE(L)                 // lift processing mode (left)
SOUS_SUS()               // compression + unification
SOUTENU(360)             // rotation paired with review
ECHO("resolve")          // interpretive residue / return value
```

- Movement = State
- Transition = Function
- Axis = Stability
- Tempo = Logic
- Echo = Return

ChoreoCode compiles physical phrases into on-chain proofs, ritual triggers, and harvest conditions.

## 4) Bell Protocol: Ritual as Throttle

Hard-coded into every harvest:

- Communal count-to-8 (synchronization)
- Full decay (no metrics, no urgency, no persistent logs)
- Right to silence at any moment

Fail-closed on perpetual ring.
The body governs the machine.

## 5) Genesis and Operational Narrative (Draft Capture)

The source text also included a launch-style narrative asserting:

- Genesis state minted (`1000 SC`) and sealed
- DAO activation
- Multi-phase corridor/NFT drop pipeline
- Base mainnet Foundry commands
- Site deployment command (`vercel --prod`)

### Included command snippets from source text

```bash
forge script drop/contracts/MintBatch.s.sol \
  --rpc-url https://mainnet.base.org \
  --broadcast --verify

vercel --prod
```

## 6) Implementation Reality Check

The source conversation correctly added a practical caveat:

- A language model cannot itself sign transactions or deploy contracts.
- It can, however, prepare complete artifacts and operator runbooks for a human signer.

That distinction is preserved here to separate:

1. **Narrative/ritual language**, and
2. **Executable, operator-owned deployment steps**.

## 7) Proposed Drop Kit Structure (as provided)

```txt
corridor-genesis/
    ├── art/
    ├── metadata/
    ├── manifests/
    ├── contract/
    │     ├── CorridorKeys.sol
    │     ├── deploy.s.sol
    │     └── verify.s.sol
    ├── site/
    ├── mirror/
    ├── origin-key/
    ├── airdrop/
    └── GENESIS_BLOCK.pdf
```

## 8) Required Operator Input

Public deployer address placeholder from source:

`0xYourAddressHere`

This draft intentionally preserves placeholder status rather than fabricating chain-side execution.

---

## Notes

This document is a clean archival reconstruction of user-provided protocol and launch copy, with structure and disclaimers added for technical clarity.
