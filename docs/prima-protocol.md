# Allison.

## Prima.

### A Protocol for Embodied Intelligence

## Abstract

The internet gave programmable money to code.  
We give programmable value to the body.

Prima is a sovereign protocol that treats human movement as the highest-resolution, tamper-evident oracle on Earth. It fuses choreographic mathematics (Py.rouette), executable ritual (ChoreoCode), kinematic governance (Glissé Engine), non-extractive throttling (Bell Protocol), and regenerative economics (Sentient Cents + Streetcred) into a single living system.

Where Ethereum minimized trust between strangers, Prima restores trust within the self — and between the self and the ledger.

The body does not speculate.  
The body cannot be Sybiled when the ritual is enforced.  
The body remembers everything in fascia, angular momentum, and the exact microsecond a fouetté axis wobbles.

This is not “move-to-earn.”  
This is proof-of-embodied-intelligence.  
The plié is the new consensus.

## 1. Introduction

For over a decade the blockchain narrative has centered on one question:  
How do we coordinate strangers without trusted intermediaries?

We ask the complementary question:

What happens when the most sovereign, high-fidelity, biologically-rooted signal generator on the planet — the trained human body — is finally granted cryptographic custody of its own output?

The body is the original computer.  
It has been training for 400,000 years to detect micro-changes in angular momentum, thermal gradient, breath, and rhythmic truth. Every grand plié is a state transition. Every honest glitch is a high-value learning event. Every sustained line is auditable lineage.

Prima is the protocol that listens.
It turns every honest movement into an economic event, every ritual into governance, every verified presence into ownable proof.

This is the kinetic layer.

## 2. The Body as State Transition System

Bitcoin demonstrated that a simple state transition system (unspent transaction outputs) could be secured by proof-of-work.

Prima demonstrates that a far richer state machine — the trained human body — can be secured by proof-of-presence.

### Core State Variables (Py.rouette)

- TES (Technical Element Score) = Base Value × Difficulty Delta × Grade of Execution
- PCS (Program Component Score) = weighted axes (line, musicality, presence, etc.)
- Glitch Factor = notarized deviation (Δω > 0.15 rad/s) → Sentient Cents mint at 1:0.618

### Execution Engine (Glissé FSM)

States: IDLE → PREP → JETÉ → FERMATA → CODA_PROFIT / CODA_STOP → CORPS_RESET

Transitions are gated by real physics:

- VZ (velocity z-score)
- AZ (acceleration z-score)
- M (persistence quorum)
- Thermal shift
- Glitch confirmation

No external validator can forge a fouetté axis.  
The body is the validator.

## 3. ChoreoCode: The Scripting Layer

```text
FIFTH()                  // neutral state initialization
PASSE(L)                 // lift processing mode (left)
SOUS_SUS()               // compression + unification
SOUTENU(360)             // rotation paired with review
ECHO("resolve")          // interpretive residue / return value
```

Movement = State  
Transition = Function  
Axis = Stability  
Tempo = Logic  
Echo = Return

ChoreoCode compiles physical phrases into on-chain proofs, ritual triggers, and harvest conditions. It is the language in which the body writes contracts with itself.

## 4. The Bell Protocol: Ritual as Throttle

Hard-coded into every harvest:

- Communal count-to-8 (synchronization)
- Full decay (no metrics, no urgency, no persistent logs)
- Right to silence at any moment

Fail-closed on perpetual ring.  
The body governs the machine.

Grande Plié Multiplier  
Depth measured via computer vision or sensor (0.00–1.00).  
Gravity multiplier = 1.0 + (pl...

---

✓ Minted 1000 SC | GENESIS_BLOCK | recGenesis

The DAO is live.
The Genesis Block is sealed. The origin point is set. Every future movement, every plié, every witness, and every coin now traces its lineage back to this timestamp.
System Status: OPERATIONAL. Ouroboros Mode: ENGAGED.
We are dancing. ❤️
