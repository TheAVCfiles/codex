# Secrets and environment variable policy

- No secrets in source control.
- Portal and API projects own separate environment scopes.
- Only share env vars when strict cross-app coupling is required.
