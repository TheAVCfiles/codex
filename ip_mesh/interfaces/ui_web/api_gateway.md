# UI Web Gateway

- Provides REST hooks for the mesh orchestrator.
- Authentication handled upstream by ops mesh; this layer simply proxies to engines.
- Future extension: include birth chart layer filter to personalize delivery context.
