[project]
name = "csv-tools"
version = "0.1.0"
description = "CSV normalization utilities for Codex Sentient Cents pipeline"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0"
]

[project.scripts]
codex-csv-normalize = "csv_tools.normalize_events:main"
